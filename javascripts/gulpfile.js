/**
 *
 * Gulp Pipeline
 *
 */

// dependencies
var gulp = require('gulp');

// asset paths
var assets = {
  scripts: [
    './plugins/*',
    './vendor/*'
  ],
  images: [
    '../assets/images/*'
  ]
};
